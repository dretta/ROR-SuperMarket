<% provide(:title, 'All Items') %>

<h1>All items</h1>

<% if  @current_user && @current_user.admin? %>
	<h2>
		<%= link_to "Create New Item",new_item_path %>
	</h2>	
<% end %>

<%= will_paginate %>
	<ul class="items">
 		<%= render @items %>
	</ul>
<%= will_paginate %>

