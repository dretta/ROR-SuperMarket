<% provide(:title, 'All Items') %>
<h1>All items</h1>

<ul class="items">
  <% @items.each do |item| %>
    <li>
      <%= link_to item.name, item %>
      <%= item.price %>
    </li>
  <% end %>
</ul>